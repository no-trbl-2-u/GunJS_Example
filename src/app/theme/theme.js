const theme = {
  // TODO: Create Palettes w/ limited colors and then convert to hex
  colors: {
    red: 'red',
    lightGrey: 'lightGrey'
  }
};

export default theme;

// IDEA: theme.palette.dark, theme.palette.sepia, theme.palette.light
// |> By using this, we can have color "families"
// |> ?? Should I do theme.dark.backgroundColor(s) and sort colors like that?
